apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: storemaker
  namespace: default
  labels:
    app: storemaker
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: storemaker
